name: kyron_design_system
description: >-
  Design tokens and the Flutter theme for the Kyron ecosystem. The single
  source of truth for colour, typography, spacing, radius and motion.
version: 0.1.0
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
